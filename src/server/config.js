module.exports = {
    database: {
      database: process.env.DATABASE_NAME || 'BlogChain',
      username: process.env.DATABASE_USER || 'sa',
      password: process.env.DATABASE_PASSWORD || 'IowvokdTik^49EL9',
    //   host: process.env.DATABASE_SERVER || 'example.database.windows.net',
    }
  };